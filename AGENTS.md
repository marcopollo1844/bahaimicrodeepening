# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Note: this project was downgraded from SDK 57 to SDK 54 so it runs in the
publicly released Expo Go app (which only supported up to SDK 54 at the
time). Revisit upgrading once Expo Go's public release supports a newer SDK.
